<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pgr.rm.RecentMapper">
	<insert id="insRecentMatch" parameterType="java.util.List">
		INSERT INTO rmatch
		( id, uid, date, name, shortname, completed )
		VALUES
		<foreach collection="list" item="item" separator=" , ">
		( #{item.id}, #{item.uid}, #{item.date}, #{item.name}, #{item.shortname}, #{item.completed})
		</foreach>
	</insert>
</mapper>